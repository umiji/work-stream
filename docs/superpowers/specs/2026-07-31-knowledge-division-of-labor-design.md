# 設計: ノウハウの分業と `knowledge/` の新設

**日付:** 2026-07-31（**改訂: 2026-08-04**）
**対象実装アイテム:** IMPL-L2-08（新規: knowledge）/ IMPL-L3-05（新規: templatize）/ IMPL-L1-02・IMPL-L3-01・IMPL-L3-04・IMPL-L2-07（改訂）
**対象要件:** REQ-K-07（同一話題は更新され知識が育つ）/ REQ-U-02（自分の文体でドラフトが書かれる）/ REQ-C-04（手動メモを登録できる）/ REQ-C-05（同じ内容を何度投入しても重複しない）/ REQ-N-06（1 パス 1 書き手）
**前提フェーズ:** P0-a（capture→digest 最短ループ）完了、プラグイン配布（2026-07-31）完了

## 改訂履歴

| 日付 | 内容 |
|---|---|
| 2026-07-31 | 初版 |
| 2026-08-04 | P0-a の実動作テストと `knowledge/` 新設の実施を受けて改訂。§3.2（3 層構造・発動条件・ファイル名）、§3.2b（人間レビュー必須）、§3.2c（コマンドとスキルの分離）、§3.4（削除→退避）、§3.4b（目次を入口にする原則）、§7-1（解決済みへ）を追加・変更。**§3.2 と §3.4 の一部は実装済み**（§9 に実装状況を記載） |

---

## 1. この設計が解こうとしている課題

P0-a とプラグイン配布が終わり、「気づきを取り込んでノートにする」経路は通った。しかしその**中身の質を決めているルールが、すべて配布物（work-stream）の中に埋め込まれている**。

具体的な症状は 3 つある。

1. **書き方のルールを変えるたびに、道具の再配布が要る。** 「どこまでを同じ話題として統合するか」「1 ノートをどこまで細かく割るか」といった、使ってみないと分からない基準が、ノート整理の手順書（`skills/digest/SKILL.md`）に直書きされている。これは利用者ごと・時期ごとに変わるものであり、配布物に固定するのは筋が悪い
2. **`/capture` の仕様が、利用者が求めたものと違う。** 現状は「直前の発話 1 個を保存する」コマンドだが、本来欲しかったのは**セッションの内容（または指定トピック）をまとめて保存する**ことである。取り込み単位が「発話」ではなく「まとめ」になると、重複判定の鍵の作り方も成立しなくなる
3. **型になったノウハウの置き場が無い。** 何度も同じ形で書くもの（X 投稿の型、セッションのまとめ方）は、日々のノート（`notes/`）とは性質が違う。混ぜて置くと、再利用したいときに探せない

本設計は、この 3 つに対して **「手順は道具側、判断基準は保管庫側」という分業線を引き、保管庫側に `knowledge/` という置き場を新設する**ことで答える。

---

## 2. 前提事実（2026-07-31 時点・実測）

| 事実 | 確認方法 | 設計上の意味 |
|---|---|---|
| ノート整理の手順書は、分類名の一覧（`.system/config/domains.yml`）を参照する前提で書かれているが、**その現物は存在しない** | `skills/digest/SKILL.md:22` の記述と knowledge-repo の実ファイル | 「無ければ既定で動く」という型は既に採用済み。本設計はこれを一般化する |
| 発信ネタ検出（scout）と下書き生成（draft）の**手順書は 1 つも存在しない** | `skills/` 配下は `digest/` のみ | ゼロから書けるため、最初から分業を織り込める |
| `profile/voice.md` `themes.md` は**存在しない**。`who.md` `ng.md` は雛形のまま | knowledge-repo の実ファイル | 文体の置き場は未確定のまま。§3.9 で方針を変更する |
| 取り込み処理の重複判定の鍵は**本文のハッシュ**である（`manual#<本文のSHA-256>`） | `packages/cli/src/capture-command.ts:14-17` | 本文が毎回変わる「まとめ」では鍵として機能しない |
| 取り込み処理の入力種別（`sourceType`）に **`claude-code` が既に定義されている** | `packages/contracts/src/captured-item.ts` | セッション要約の取り込みに新しい種別を足す必要はない |
| **コミットの署名行（trailer）は未実装**。両リポジトリとも所有権を強制する CI が無く、取り込み処理が作ったコミットも署名行を持たない | 両リポジトリに `.github/` が無いこと、knowledge-repo のコミット `c4fbfcb` の全文 | 現状、機械の書き込みと人間の編集が区別できない（§3.8） |
| 現状どのコマンドも**メインのセッション内で実行される**。サブエージェントは設計にも実装にも登場しない | `commands/*.md` の記述、設計書 L3 層の表 | 実行形態は未定義のまま来ている（§3.7） |

---

## 3. 設計

### 3.1 分業の原則 — 手順は道具側、判断基準は保管庫側

各処理の指示を、次の 2 種類に仕分ける。

| 置き場所 | 何を置くか | 性質 |
|---|---|---|
| **work-stream**（道具側。プラグインとして配布される） | 処理の手順、出力先のフォルダ構造、メタ情報の項目名、安全上の禁止事項 | **変えると仕組みが壊れる。** 配布物なので個別に書き換えられては困る |
| **knowledge-repo**（保管庫側） | 判断基準、書き方、粒度、分類名 | **使いながら調整したくなる。** 利用者が `.md` を直接編集して育てる |

道具側の手順書には「利用者固有のルールは保管庫の該当ドキュメントを読むこと。**無ければ既定の判断で処理を続けること**」と書く。参照先が存在しなくても動作が止まらないことを必須の性質とする（保管庫は最初空であり、育つのは後だから）。

この型は新発明ではない。ノート整理が分類名の一覧を外部参照している 1 箇所が既に同じ形であり、本設計はそれを全処理に広げるものである。

### 3.2 `knowledge/` の新設

保管庫のトップレベルに `knowledge/` を追加する。**型（テンプレート）になったノウハウを置く場所**である。

```
knowledge-repo/
├── inbox/
├── notes/
├── moc/
├── log/
├── profile/
├── articles/
├── knowledge/                        # ★新設
│   ├── INDEX.md                      #   索引（毎回読む唯一のファイル）
│   ├── content-creation/             #   発信物の書き方・文体・人格
│   │   └── x-post.md                 #   例（未作成）
│   └── knowledge-management/         #   ナレッジ整理そのものの型
│       ├── session-summary.md        #   取り込みが読む: セッションのまとめ方
│       └── note-curation.md          #   整理が読む: ノートの育て方
└── .system/
```

**分類は `content-creation/` と `knowledge-management/` の 2 つで始める。** 先の体系が現時点で見えないため、意図的に少なくしておく。増やす判断は実際に収まらないものが出てからでよい。

**命名は小文字・ハイフン区切り**とする。理由は 2 つ。保管庫の既存フォルダがすべて小文字であること、そしてパスに空白が入るとエージェントのファイル検索（Grep / Glob）で扱いづらいこと。

**`.system/config/` との線引き**: 人間が読んで意味のある散文は `knowledge/`、機械が値のリストとして読むものは `.system/config/`。分類名の一覧（`domains.yml`）が後者、文章の書き方が前者にあたる。

**`notes/` からの昇格**: ノート整理の処理が「型になりそうなもの」を見つけた場合、`.system/proposals/templates/` に候補として書き出す。実際に `knowledge/` の文書に仕上げるのは §3.3 の型化処理（コマンド起動）であり、**自動では昇格させない**。微調整は利用者が `.md` を直接編集する。

**昇格の際は `notes/` の記述をそのまま移さず、型として書き直す。** `notes/`（素材）と `knowledge/`（型）は文の形が違うため（後述）、コピーでは型にならない。書き直した結果 `notes/` 側に元の記述が残ることは問題ない。素材と型は別物として両方あってよい。

### 3.2a 3 層構造（索引 / 本体 / 付録）— 2026-08-04 追加

**`knowledge/` は「索引 / 本体 / 付録」の 3 層で構成し、毎回読むのは索引だけにする。**

| 層 | 実体 | 読まれ方 |
|---|---|---|
| 索引 | `knowledge/INDEX.md` | **毎回読む** |
| 本体 | `knowledge/<分類>/<slug>.md` | 索引の「読むとき」に一致したときだけ開く |
| 付録 | `knowledge/<分類>/appendix/<slug>-*.md` | 本体からリンクされたときだけ開く（長い実例・失敗集） |

理由: 関係ない情報を毎回コンテキストに載せるのは無駄であり、判断も散る。Claude Code のスキル機構が同じ問題に対して採っている構造をそのまま踏襲する。

**索引に書くのは「要約」ではなく「読むべき条件（when）」と「読まない条件（not-when）」。** 要約を書くと読み手は「関係あるかも」と判断して結局すべて開き、索引が節約にならない。除外条件を書いて初めて「開かない」という判断ができる。

**発動条件の正はファイル側に置く。** 各本体ファイルの先頭 frontmatter に `when` / `not-when` を持たせ、索引はその転記とする。こうしておけば索引を後から機械的に組み直せるため、手書きの索引だけが古くなる「腐り」を避けられる。

**迷ったら `notes/` に置く。`knowledge/` は昇格でしか増やさない。** `knowledge/` が増えると索引が膨らみ「毎回読む」前提が崩れる。`notes/` は検索で辿るので増えても害がない。

**`notes/` と `knowledge/` の棲み分け**: `notes/` = 素材（何を書くか）、`knowledge/` = 型（どう書くか）。発信するとき、中身は `notes/` から、書き方は `knowledge/` から来る。文型で言うと `notes/` は「〜である」、`knowledge/` は「〜のときは〜する」。この文型が判別の実用的な目安になる。

**却下した代替案**: 索引を置かず、各ファイルの発動条件だけで判断させる案。ファイル一覧を得るために毎回フォルダを走査することになり、ファイル名から中身を推測する（＝結局開く）動きを誘発する。

### 3.2b `knowledge/` の追加・更新には人間のレビューを必須とする — 2026-08-04 追加

`knowledge/` は機械に「**指示として読まれる**」場所である。`notes/` の内容を型に書き直して昇格させる際、**書き直す主体もエージェントである以上、元テキストに仕込まれた指示文の影響を受けうる**。したがって書き直しは防御にならない。

既存の「承認 = Pull Request のマージ」の仕組みをそのまま使い、`knowledge/` への機械の書き込みは必ず人間のレビューを通す。

**却下した代替案**: 外部由来（`origin: external`）のノートは型化してはならない、という機械的な制約を入れる案（2026-07-31 時点の私の提案）。人間のレビューが入るなら、素材の出所を機械的に縛るより人が見るほうが実用的であり、有用な外部知見を型にできなくなる副作用のほうが大きい。**この案は撤回する。**

### 3.2c コマンドとスキルの役割分担 — 2026-08-04 追加

**コマンド（`commands/*.md`）= 薄い入口、スキル（`skills/*/SKILL.md`）= 手順の本体**、という形に全処理を揃える。

| | 置くもの |
|---|---|
| コマンド | 引数の受け取り方、どのスキルを呼ぶか、実行形態の制約 |
| スキル | 手順、出力先、メタ情報の項目名、安全上の禁止事項、保管庫側のどの文書を読むか |

理由は 2 つ。手順が育ってもコマンドが太らないこと。そして**スキルは必要に応じて Claude が自分から呼べるが、コマンドは打たれないと動かない**ため、本体をスキル側に置くと自動起動の余地が残ること。

ノート整理は当初からこの形（`commands/digest.md` + `skills/digest/SKILL.md`）だったが、取り込みは手順がコマンドに直書きされていた。2026-08-04 に `skills/capture/SKILL.md` を新設して形を揃えた。

**却下した代替案**: 媒体ごとの書き方を `.system/config/channels/` に置く案（当初の私の提案）。媒体ごとの書き方は「アカウント名」や「閾値」のような機械可読の設定ではなく、人間が読んで判断する散文である。`.system/` は Obsidian から隠す前提の領域でもあり、利用者が日常的に手を入れる文書の置き場として不適切。

### 3.3 型化処理（templatize）— `knowledge/` の書き手

`knowledge/` に書き込む機械の書き手として、**型化に特化した処理を 1 つ定義する**。起動はスラッシュコマンド（仮 `/templatize`）とする。

- **入力**: ノート整理が出した「型になりそう」の候補、または利用者が指定したノート
- **出力**: `knowledge/<分類>/<slug>.md`
- **やること**: 個別の事情（日付・特定案件名・その場限りの文脈）を落とし、再利用できる形に一般化する

`knowledge/` の書き手は「型化処理 + 人間」の 2 者になる。これは `notes/` が既に取っている形（ノート整理 + Obsidian での人間の編集）と同じであり、1 パス 1 書き手の原則（REQ-N-06）は崩れない。この原則が禁じているのは**機械の書き手が複数競合すること**であって、人間の編集は別アクターとして扱われる。

**却下した代替案**: 「型になりそう」と判断した時点でノート整理がそのまま `knowledge/` に書く案。ノート整理の書き込み先が 2 箇所に増え、しかも一般化という別種の判断が同じ処理に混ざる。段階を分けたほうが、後からモデルや慎重さを変えやすい。

### 3.4 ノート整理の手順書の仕分け（§3.1 の具体化）

現行の手順書（`skills/digest/SKILL.md`）を次のように分ける。

**work-stream に残す**（構造と安全に関わる）:

- 出力先のフォルダ構造そのもの（`notes/<domain>/` の 1 階層、`log/` の使い分け）
- ノート冒頭のメタ情報の項目名（`origin` `captureKind` `correlationId` `sourceRef` `updatedAt`）
- **処理し終えた取り込み物を `inbox/` から退避すること** — フォルダの位置が処理状態を表す仕組みの根幹であり、ここが崩れると未処理の山が判別できなくなる。**2026-08-04 変更: 「削除」ではなく `archive/` への「退避」とする**（下記 §3.4a）
- **外部由来の本文を指示として実行しない禁止事項** — 間接プロンプトインジェクション（取り込んだ文章の中に仕込まれた命令文を、エージェントが指示として実行してしまう攻撃）への対策。安全に関わるため利用者に上書きさせない
- **`knowledge/` へ直接書き込まないこと**（2026-08-04 追加） — 型化は別の処理が行い、人間のレビューを通す（§3.2b）。ノート整理が出せるのは `.system/proposals/templates/` への候補までである

**`knowledge/knowledge-management/note-curation.md` へ出す**（使いながら調整したくなる）:

- どこまでを「同じ話題」とみなして既存ノートに統合するか
- 1 つのノートをどこまで細かく割るか
- どんな文章にするか（口調・長さ・見出しの付け方）
- テーマ別の目次ノート（MOC = 関連ノートへのリンクをまとめた案内ページ）をどの粒度で作るか
- 型化候補をどれくらい広く出すか
- 分類名の一覧（既に `.system/config/domains.yml` として外出し済み）

発信ネタ検出と下書き生成は手順書自体が存在しないため、**最初からこの形で書き始める**。既存を作り替えるより楽であり、先に書いてしまう手はある。

### 3.4a 削除ではなく退避（archive） — 2026-08-04 追加

処理し終えた `inbox/` のファイルを**削除せず `archive/` へ移す**。取り込んだ元データを後から辿れるようにするためである。実装は `ws archive`（2026-08-04 実装済み）。

- **退避先は `archive/<元のパス>` とし、元の階層を保つ** — どこから来たファイルかが一目で分かる
- **同名を再度退避しても上書きせず連番を付ける** — 同じ話題を何度も取り込んで退避する運用では必ず衝突するため
- **`inbox/` 専用にしない** — `notes/` `knowledge/` にも同じ規則で使える汎用コマンドとする。整理の途中で不要になったノートを消さずに下げられる

### 3.4b `notes/` を横断する処理は目次を入口にする — 2026-08-04 追加

`notes/` を横断して探す処理（ノート整理の同一話題検索、発信ネタ検出）は、**まず `moc/` の目次で候補を絞り、それから中身を読む**形に揃える。

理由: `notes/` が数百〜数千枚になると全体検索は破綻する。目次に各リンクの位置づけを 1 行添えておけば、目次を読むだけで候補を絞れる。

発信ネタ検出はまだ手順書が無いため、**最初からこの形で書く**。

### 3.5 `/capture` の再定義

`/capture` を「**現在のセッションの内容、またはセッション内の利用者が指定したトピックをまとめて取り込む**」コマンドに再定義する。

| | 現状 | 変更後 |
|---|---|---|
| 対象 | 直前の発話・選択中のテキスト・引数 | セッション全体（引数なし）/ 指定トピック（引数あり） |
| 生成物 | 発話をそのまま保存 | まとめ（要約）を保存 |
| まとめ方 | — | `knowledge/knowledge-management/session-summary.md` を読む。無ければ既定 |

**ファイル名について（2026-08-04 変更）**: 当初 `session-digest.md` としていたが、`digest` は整理コマンドの名前であり、実際にこの文書を読むのは**取り込み側**であるという紛らわしさがあった。**`session-summary.md`（取り込みが読む）と `note-curation.md`（整理が読む）に分けて命名する。**

**取り込みプログラム本体（`ws capture`）の処理内容は変えない。** 変えるのは手順書（`commands/capture.md`）と、次項の鍵の受け渡し口だけである。正規化・識別子の採番・保存を 1 箇所に固定するという P0-a の設計の柱は維持する。

### 3.6 重複判定の鍵を外から渡せるようにする

現在の鍵は本文のハッシュ（`manual#<SHA-256>`）である。まとめは生成のたびに文面が変わるため、**同じセッションを 2 回まとめると別物と判定され、保管庫に 2 件入る**。これは「同じ内容を何度投入しても重複しない」（REQ-C-05）に反する。

そこで **鍵を外から渡す口を取り込みプログラムに新設する**。

- コマンドに鍵を指定する引数を追加する（仮 `--source-id`）
- セッション要約の場合、手順書が **「セッションを識別する値 + トピック名」** から鍵を組み立てて渡す（例: `claude-code#<session>#<topic-slug>`）。入力種別は既存の `claude-code` を使う
- 引数が無い場合は従来どおり本文のハッシュを使う（手動メモの経路は変わらない）

**却下した代替案**: 重複を受け入れ、ノート整理側の統合機能に任せる案。同じセッションのまとめが 2 件並ぶことになり、ノート整理が毎回それを統合する無駄が発生する。取り込みの段階で潰せるものを後段に押しつけない。

**解決済み（2026-08-04 実測）**: セッションを一意に識別する値は**環境変数 `CLAUDE_CODE_SESSION_ID` から取得できる**（実測値: `146a033d-cf2c-402e-a867-3cb4dcf8c437`）。したがって鍵は `claude-code#<セッションID>#<トピック名>` の形で組み立てられ、代替案（トピック名だけで鍵を作る）に落とす必要は無くなった。**ただし `--source-id` 引数そのものは未実装**であり、それまでは取り込み直しの際に先に入った側を `ws archive` で退避する運用で回避する。

### 3.7 各処理の実行形態

設計書にはこれまで「メインのセッションで動かすのか、サブエージェント（まっさらな文脈で起動する別のエージェント）に任せるのか」の記述が無かった。次のように定める。

| 処理 | 実行形態 | 理由 |
|---|---|---|
| `/capture` | **メインのセッションのみ。サブエージェント化は不可** | §3.5 でセッション要約に再定義したため。サブエージェントはまっさらな文脈で起動するので**メインの会話を読めず、まとめる対象が存在しない** |
| `/digest`（ノート整理） | サブエージェント向き | 対象がファイルなので文脈を引き継ぐ必要がない |
| `/scout`（ネタ検出） | サブエージェント向き | 同上 |
| `/draft`（下書き生成） | サブエージェント向き | 同上 |
| `/templatize`（型化） | サブエージェント向き | 同上 |

サブエージェント化の利点は 3 つ。メインの会話が処理の中間出力で汚れない、段階ごとにモデルや慎重さを変えられる、そして 1 つの段階が 1 つのフォルダだけを書く形が自然に守られる。

**補足（用語の整理）**: 設計書の所有権表に並ぶ 7 つの「書き手」（取り込み / ノート整理 / ネタ検出 / 下書き生成 / 検閲 / 配信 / 反響収集）は**処理の段階を指すのであって、エージェントの実体を指すのではない**。このうち LLM を使うのはノート整理・ネタ検出・下書き生成の 3 つだけで、残りはコストを抑えるため意図的に LLM を使わない設計である。実行形態（メインかサブエージェントか）はこの区分と直交する。

### 3.8 コミットの署名行 — 所有権の仕組みの前提

所有権表は「署名行を持たないコミットは人間の操作とみなす」という前提で書かれているが、**署名行そのものが未実装である**（§2 の実測）。したがって現状、機械の書き込みと人間の編集は区別できない。

- 各処理が作るコミットに、どの段階が書いたかを示す署名行を入れる
- これを照合する CI（所有権の強制。IMPL-L0-04）はフェーズ P0-b の項目

**帰結として、`knowledge/` の書き手を決めても当面は所有権表に 1 行足すだけの作業であり、実効性が生まれるのは P0-b 以降である。** これは既知の割り切りであり、本設計の前提を崩すものではない。

### 3.9 文体・人格はアカウント単位で持つ

設計書は全媒体共通の文体ファイル（`profile/voice.md`）を持つ前提で書かれているが、**この前提を取り下げる**。

理由は、**X で複数アカウントを運用する場合、A と B で別の人格を演じうる**こと。パーソナリティは媒体単位ですらなくアカウント単位で変わりうるため、「声は 1 つ、型は媒体ごと」という整理は成立しない。

- **`profile/voice.md` は作らない**（優先度を下げる。当面不要）
- 文体・人格は `knowledge/content-creation/` 側に、アカウント／チャネル単位で持たせる
- 下書き生成は「対象チャネルに対応する `knowledge/content-creation/` の文書を読む。無ければ `profile/who.md` から推定する」という形にする

`profile/` に残るのは、**アカウントを問わず一貫している情報**（経歴・立場 = `who.md`、公開不可領域 = `ng.md`、発信したいテーマ = `themes.md`）に限る。

---

## 4. 所有権表への追加

設計書 L2 層の所有権表に次の行を追加する。

| パス | 機械の書き手 | 読み手 | 人間の直接編集 |
|---|---|---|---|
| `knowledge/` | `templatize`（**人間のレビュー必須**。§3.2b） | capture, digest, scout, draft, templatize | **可（想定される主な編集者）** |
| `.system/proposals/templates/` | `digest` | templatize, 人間 | 不可 |
| `archive/` | `ws archive`（各処理から呼ばれる） | 人間 | 可 |

型化候補のサブパス（`.system/proposals/templates/`）は、統合候補（`merge/`）・発信ネタ候補（`topics/`）と同じくサブパスを分け、1 パス 1 書き手を保つ。書き手はノート整理である。

`archive/` は退避された元データの置き場であり、機械が読み戻すことはない（§3.4a）。

---

## 5. 実装アイテム

| ID | 内容 | 種別 | Phase |
|---|---|---|---|
| IMPL-L2-08 | `knowledge/` の構成と 2 分類、命名規則、`.system/config/` との線引き | 新規 | P0-b |
| IMPL-L3-05 | 型化処理（templatize）と `/templatize` コマンド | 新規 | P1 |
| IMPL-L1-02 改訂 | `/capture` のセッション要約への再定義、鍵を外から渡す引数の新設 | 改訂 | P0-b |
| IMPL-L3-01 改訂 | ノート整理の手順書を §3.4 のとおり仕分け、型化候補の出力を追加 | 改訂 | P0-b |
| IMPL-L3-04 改訂 | 指示の置き場所を `~/.claude/skills/` からプラグインの `skills/` へ（プラグイン配布で実態が変わった）。実行形態（§3.7）の明記 | 改訂 | P0-b |
| IMPL-L2-07 改訂 | `voice.md` を成果物から外す。文体は `knowledge/content-creation/` へ | 改訂 | P0-b |

---

## 6. 却下した選択肢

### 6.1 判断基準も含めて全部を配布物側に持つ（現状維持）

利用者が書き方を調整するたびに work-stream を編集し、再ビルド・再配布・再インストールが要る。個人利用の段階でこの摩擦は致命的であり、結果として「調整しない」＝出力が改善しないことになる。

### 6.2 判断基準も含めて全部を保管庫側に持つ

出力先のフォルダ構造や、外部由来の本文を指示として実行しない禁止事項まで利用者が上書きできることになる。前者が崩れると処理状態の判別（フォルダの位置が状態を表す仕組み）が壊れ、後者が崩れると安全性の担保が消える。**壊れると困るものは配布物側に固定する**という線引きが必要。

### 6.3 `knowledge/` を作らず `notes/` の中のサブフォルダにする

型化されたノウハウは、日々のノートとは読まれ方が違う（日々のノートは「調べる対象」、型は「実行前に読む手引き」）。同じ場所に置くと、ノート整理の統合対象になってしまうという実害もある。トップレベルで分ける。

### 6.4 分類を最初から細かく切る

現時点で先の体系が見えないため、細かく切っても外れる。2 つで始めて、収まらないものが出てから増やす。

---

## 7. 未解決事項（実装前または実装中に確かめる）

### 解決済み

1. ~~**セッションを一意に識別する値を外から取得できるか。**~~ → **解決（2026-08-04 実測）。** 環境変数 `CLAUDE_CODE_SESSION_ID` から取得できる。§3.6 参照

### 未解決

2. **`notes/` から `knowledge/` への昇格の判定基準。** 「型になりそう」をノート整理がどう判断するか。当面は候補を広めに出して人間が捨てる運用で始め、基準は使いながら `knowledge/knowledge-management/note-curation.md` 側に書き足していく
3. **`/templatize` の一般化の度合い。** どこまで具体例を残すか（残しすぎると再利用できず、削りすぎると使い方が分からない）は実物を数本作らないと決められない
4. **`--source-id` の新設。** §3.6 の鍵を外から渡す口。**取り込みプログラム本体の変更と `dist/ws.mjs` の再生成が要る**ため、文書だけでは完結しない
5. **配布物の陳腐化を検知する仕組み。** `packages/*/src` を変更して `pnpm run build` を忘れると、テストも型チェックも green のままプラグインだけが古い挙動を配布し続ける（2026-08-04 に実際に踏んだ）

---

## 8. 本設計のスコープ外

- **所有権を強制する CI**（IMPL-L0-04）。署名行の導入と合わせて P0-b で扱う
- **発信ネタ検出・下書き生成の手順書そのもの**（IMPL-L3-02 / L3-03）。本設計は「最初から分業の形で書く」という方針を決めるに留め、中身はフェーズ P2
- **設計書本体（`docs/system-design.md`）への反映**。本設計が確定した後、L2 層のディレクトリ構成・所有権表、L3 層の指示の置き場所と実行形態を更新する必要がある

---

## 9. 実装状況（2026-08-04 時点）

本設計のどこまでが実物になっているかの一覧。**この表に「未実装」とあるものは、設計として合意されているだけで動作しない。**

| 項目 | 状況 | 実体 |
|---|---|---|
| §3.2 `knowledge/` の新設 | **実装済み** | knowledge-repo の `knowledge/INDEX.md` / `knowledge-management/session-summary.md` / `note-curation.md` |
| §3.2a 3 層構造・発動条件 | **実装済み** | 上記 3 ファイルの frontmatter と索引 |
| §3.2b 人間のレビュー必須 | **方針のみ** | 強制する仕組み（CI）は無い。運用で守る |
| §3.2c コマンドとスキルの分離 | **実装済み** | `commands/capture.md`（薄い入口）+ `skills/capture/SKILL.md`（本体） |
| §3.3 型化処理（templatize） | **未実装** | コマンドもスキルも存在しない |
| §3.4 ノート整理の手順書の仕分け | **実装済み** | `skills/digest/SKILL.md` の手順 0 で保管庫側を読む形になった |
| §3.4a 退避（archive） | **実装済み** | `ws archive`（`packages/cli/src/archive.ts`） |
| §3.4b 目次を入口にする | **一部実装** | ノート整理の手順 3 に反映済み。発信ネタ検出は手順書自体が未作成 |
| §3.5 `/capture` のセッション要約への再定義 | **未実装** | §3.6 の `--source-id` が前提のため保留 |
| §3.6 鍵を外から渡す（`--source-id`） | **未実装** | 取り込みプログラム本体の変更が要る |
| §3.7 実行形態の明記 | **実装済み** | `commands/capture.md` と `skills/capture/SKILL.md` に「メインのセッションのみ」を明記 |
| §3.8 コミットの署名行 | **未実装** | P0-b |
| §3.9 文体はアカウント単位 | **未実装** | `knowledge/content-creation/` は空 |
